import "../s-nav";
import "../s-button";
export interface SDetailsProps {
    summary?: string;
    content?: string;
}
export declare const SDetails: ({ summary, content }: SDetailsProps) => import("lit-html").TemplateResult<1>;
